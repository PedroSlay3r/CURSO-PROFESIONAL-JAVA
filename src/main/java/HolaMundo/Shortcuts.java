package HolaMundo;

public class Shortcuts {

    // Ctrl + space → Para ver las mútliples lsitas de sugerencias
    // Ctrl + e → Para eliminar una línea de código
    // Ctrl + shift + c → Para comentar un alínea de código con //
    // Ctrl + shift + f → Para encontrar un proyecto
    // Ctr l + TAB → Para cambiar de diferentes pestañas
    // Ctrl + shit + i → Para importar liberías
    // Alt + insert → Lista de posible código a usar
    // Shift + Ctrl + O → Abrir proyecto

    // Alt + shift + f → Para identar el código
    // Altr + shift + (↑ ↓ )

    // psvm → Para crear un objeto de la clase main
    /*
     * public static void main(String[] args) {
     * }
     */

    // sw → Para autocomletar el swich, crear un caso y uno por default
    // for → Para crear un ciclo for
    // if → Para craer crear una condición
    // wh → Para crear un

    // psvm → Para auto completar <public static void main(String[] args) {}

    public static void main(String[] args) {
        // sw → Para autocomletar el swich, crear un caso y uno por default
        /*
         * switch (var) {
         * case val:
         * 
         * break;
         * default:
         * throw new AssertionError();
         * }
         */

        // for → Para crear un ciclo for
        /*
         * for (int i = 0; i < 10; i++) {
         * 
         * }
         */

        // Para crear una estrucutura condicional
        /*
         * if (true) {
         * 
         * }
         */

        // Para crear una estrucura while
        /*
         * while (true) {
         * 
         * }
         */
    }

}
